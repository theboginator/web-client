import { Alert, AlertIcon } from "@chakra-ui/alert";

export default function NoResults() {
    return (
        <Alert status="info">
                    <AlertIcon />
                    No results
            </Alert>
    );;;
}
